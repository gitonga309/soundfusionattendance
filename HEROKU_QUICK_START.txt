# 🚀 HEROKU DEPLOYMENT - QUICK START (5 MINUTES)

## Your app is ready! Here's the fastest way to get live:

### Step 1: Install Heroku CLI
```powershell
choco install heroku-cli
```
Or download: https://devcenter.heroku.com/articles/heroku-cli

### Step 2: Generate SECRET_KEY
```powershell
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```
Copy the output.

### Step 3: Deploy!
```bash
# Login to Heroku
heroku login

# Create app
heroku create soundfusion-attendance

# Add database
heroku addons:create heroku-postgresql:mini --app soundfusion-attendance

# Set environment variables
heroku config:set DEBUG=False --app soundfusion-attendance
heroku config:set SECRET_KEY="paste-your-secret-key-here" --app soundfusion-attendance
heroku config:set ALLOWED_HOSTS="soundfusion-attendance.herokuapp.com" --app soundfusion-attendance

# Deploy code
heroku git:remote -a soundfusion-attendance
git push heroku main

# Run migrations
heroku run python manage.py migrate --app soundfusion-attendance

# Create admin user
heroku run python manage.py createsuperuser --app soundfusion-attendance
```

### Step 4: Go Live! 🎉
Visit: **https://soundfusion-attendance.herokuapp.com**

---

## That's it!

Your app is now live. All users can access it.

For detailed instructions, see `HEROKU_DEPLOYMENT.md`

Questions? Run:
```bash
heroku logs -t --app soundfusion-attendance
```
